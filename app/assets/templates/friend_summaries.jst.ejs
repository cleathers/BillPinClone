<% if (!(_.isEmpty(positives))) { %>
  These peeps owe me
  <table class="table table-striped">
    <% _.each(positives, function(amt, user_id) { %>
      <tr>
        <td><%= users.get(user_id).escape('email')  %></td>
        <td><%= amt %></td>
      </tr>
    <% }); %>
  </table>
<% } %>

<% if (!(_.isEmpty(negatives))) { %>
  I owe these people
  <table class="table table-striped">
  <% _.each(negatives, function(amt, user_id) { %>
      <tr>
        <td><%= users.get(user_id).escape('email')  %></td>
        <td><%= amt %></td>
      </tr>
    <% }); %>
  </table>
<% } %>
